
# سامانه جامع مدیریت مصالح (Construct Pro)

این پروژه یک سامانه کامل برای مدیریت ورود و خروج مصالح ساختمانی، صدور فاکتور و انبارداری است.
این نسخه از پروژه به صورت **Full-Stack** طراحی شده و شامل فرانت‌‌اند (React) و بک‌اند (Node.js + SQLite) می‌باشد. دیتابیس به صورت داخلی (Local) بوده و نیاز به نصب نرم‌افزار جانبی ندارد.

---

## 📋 پیش‌نیازها (Prerequisites)

قبل از شروع، مطمئن شوید که سیستم عامل شما **Ubuntu 20.04** یا بالاتر است. دستورات زیر ابزارهای اولیه، گیت و Node.js را نصب می‌کنند.

### ۱. به‌روزرسانی سیستم و نصب ابزارهای پایه
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install curl git build-essential -y
```

### ۲. نصب Node.js (نسخه LTS)
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### ۳. بررسی صحت نصب
```bash
node -v
npm -v
```
*(باید نسخه Node v20.x.x و NPM را مشاهده کنید)*

---

## 🚀 نصب و راه‌اندازی پروژه

ترمینال را در پوشه‌ای که فایل‌های پروژه قرار دارند باز کنید و مراحل زیر را دنبال کنید.

### ۱. ایجاد فایل تنظیمات پکیج (اگر وجود ندارد)
از آنجا که فایل `package.json` در فایل‌های ارسالی نبود، با دستور زیر آن را می‌سازیم:

```bash
npm init -y
```

### ۲. نصب وابستگی‌های بک‌اند (Backend Dependencies)
این دستورات کتابخانه‌های مورد نیاز سرور و دیتابیس را نصب می‌کنند:

```bash
npm install express sqlite3 cors uuid
```

### ۳. نصب وابستگی‌های فرانت‌اند (Frontend Dependencies)
برای اجرای کدهای React (فایل‌های `tsx.`) نیاز به ابزار Vite داریم:

```bash
npm install vite @vitejs/plugin-react react react-dom recharts -D
```

---

## 🗄️ راه‌اندازی دیتابیس (Database Setup)

این پروژه از **SQLite** استفاده می‌کند.
*   **مزیت:** نیاز به نصب سرور دیتابیس جداگانه (مثل MySQL یا Postgres) **نیست**.
*   **روش ساخت:** دیتابیس و جداول به صورت **خودکار** در اولین اجرای سرور ساخته می‌شوند.
*   **فایل دیتابیس:** یک فایل با نام `database.sqlite` در پوشه پروژه ایجاد خواهد شد.

---

## ⚙️ تنظیمات و اجرا (Execution)

برای اجرای کامل سیستم، باید دو ترمینال جداگانه باز کنید: یکی برای سرور (بک‌اند) و یکی برای رابط کاربری (فرانت‌اند).

### ترمینال اول: اجرای بک‌اند (Server)
این دستور سرور را روی پورت ۳۰۰۱ روشن کرده و دیتابیس را می‌سازد.

```bash
node server.js
```
*خروجی مورد انتظار:*
> Connected to the SQLite database.
> Server running on http://localhost:3001

---

### ترمینال دوم: اجرای فرانت‌اند (Client)
یک تب جدید در ترمینال باز کنید (یا `Ctrl+Shift+T` بزنید) و دستور زیر را اجرا کنید:

```bash
npx vite
```
*نکته: اگر سوالی پرسیده شد، `y` را بزنید تا پکیج vite نصب شود.*

*خروجی مورد انتظار:*
> VITE v5.x.x  ready in x ms
> ➜  Local:   http://localhost:5173/

---

## ✅ بررسی صحت نصب

۱. مرورگر خود را باز کنید.
۲. به آدرس زیر بروید:
   **http://localhost:5173**
3. صفحه ورود باید نمایش داده شود.

**اطلاعات ورود پیش‌فرض (مدیر سیستم):**
*   **نام کاربری:** `admin`
*   (نیاز به رمز عبور ندارد یا در کد تعریف نشده است)

---

## 🛠️ دستورات مفید و عیب‌یابی

### فایل `index.html`
اگر فرانت‌اند صفحه سفید نشان داد، مطمئن شوید فایل `index.html` حاوی خط زیر در داخل تگ `<body>` باشد تا فایل اصلی پروژه را فراخوانی کند (اگر نیست، دستی اضافه کنید):
```html
<script type="module" src="/index.tsx"></script>
```

### خطای پورت (Port in use)
اگر خطای `EADDRINUSE` دریافت کردید، یعنی پورت ۳۰۰۱ اشغال است. پروسه قبلی را ببندید:
```bash
sudo lsof -i :3001
# PID پروسه را پیدا کنید و دستور زیر را بزنید (مثلا اگر PID برابر 1234 بود)
kill -9 1234
```

### بازنشانی دیتابیس
اگر خواستید تمام داده‌ها پاک شود و سیستم به حالت اول برگردد:
1. سرور را متوقف کنید (`Ctrl+C`).
2. فایل دیتابیس را حذف کنید:
```bash
rm database.sqlite
```
3. سرور را مجددا اجرا کنید (`node server.js`).

