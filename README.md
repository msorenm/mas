
# سامانه جامع مدیریت مصالح (Construct Pro)

این پروژه یک پلتفرم Full-Stack برای مدیریت ورود و خروج مصالح، انبارداری و صدور فاکتور است که با تکنولوژی‌های مدرن وب توسعه یافته است.

---

## 🛠️ تکنولوژی‌های استفاده شده

*   **Frontend:** React, Vite, TailwindCSS, Recharts
*   **Backend:** Node.js, Express
*   **Database:** SQLite (Embedded)

---

## 📋 پیش‌نیازها

برای اجرای این پروژه، به موارد زیر روی سیستم خود نیاز دارید:
1.  **Node.js**: نسخه 18 یا بالاتر.
2.  **Git**: برای دریافت کدها.

---

## 🚀 راهنمای نصب و راه‌اندازی سریع

برای نصب و اجرای پروژه روی سیستم عامل اوبونتو (یا مک/ویندوز)، دستورات زیر را به ترتیب در ترمینال اجرا کنید.

### ۱. دریافت پروژه (Clone)
ابتدا مخزن پروژه را دریافت کرده و وارد پوشه آن شوید:

```bash
git clone https://github.com/msorenm/mas.git
cd mas
```

### ۲. نصب وابستگی‌ها (Install Dependencies)
تمام کتابخانه‌های مورد نیاز فرانت‌اند و بک‌اند را با یک دستور نصب کنید:

```bash
npm install
```

### ۳. اجرای سامانه (Run)
برای سهولت، یک دستور واحد برای اجرای همزمان سرور و کلاینت تنظیم شده است:

```bash
npm start
```

این دستور کارهای زیر را انجام می‌دهد:
*   سرور بک‌اند را روی پورت **3001** روشن می‌کند.
*   دیتابیس `database.sqlite` را به صورت خودکار می‌سازد.
*   سرور فرانت‌اند را روی پورت **5173** اجرا می‌کند.

پس از مشاهده پیام‌های موفقیت‌آمیز، مرورگر خود را باز کنید و به آدرس زیر بروید:
👉 **http://localhost:5173**

---

## 🔐 اطلاعات ورود پیش‌فرض

برای اولین ورود می‌توانید از نام کاربری مدیر سیستم استفاده کنید:

*   **نام کاربری:** `admin`
*   **سطح دسترسی:** مدیر ارشد (Super Admin)

---

## ⚠️ عیب‌یابی (Troubleshooting)

### خطای "Failed to fetch" یا "Server not ready"
این خطا یعنی فرانت‌اند نمی‌تواند به بک‌اند متصل شود.
1.  مطمئن شوید دستور `npm start` در حال اجراست و ترمینال بسته نشده است.
2.  اگر پورت 3001 اشغال باشد، سرور اجرا نمی‌شود. با دستور زیر پورت را آزاد کنید:
    ```bash
    # پیدا کردن پروسه درگیر
    sudo lsof -i :3001
    # بستن پروسه (PID را جایگزین کنید)
    kill -9 <PID>
    ```

### صفحه سفید در مرورگر
اگر صفحه بارگذاری نمی‌شود، کنسول مرورگر (F12 > Console) را چک کنید. اگر خطای مربوط به `import` دیدید، مطمئن شوید فایل `index.html` درست است و `importmap` حذف شده است.

### بازنشانی دیتابیس
برای پاک کردن تمام داده‌ها و شروع مجدد:
```bash
rm database.sqlite
npm start
```
